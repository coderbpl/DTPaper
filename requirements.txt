# DarkTrace Phase 1 — CPU-only, no GPU required
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
imbalanced-learn>=0.11   # SMOTE (manuscript ref [25])
matplotlib>=3.7          # figures (confusion matrices, PR curves)
pyyaml>=6.0              # optional; JSON configs work without it
pytest>=7.0              # optional; smoke tests
datasets>=2.14           # optional; only to export CoDA from Hugging Face
# Phase 2+ only (NOT needed for Phase 1):
# torch>=2.0
# transformers>=4.35
# shap>=0.43
# lime>=0.2
# Phase 3 (explainable scoring) — CPU OK
shap>=0.44
lime>=0.2
