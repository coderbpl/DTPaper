# DarkTrace Phase 1 — CPU-only, no GPU required
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
imbalanced-learn>=0.11   # SMOTE (manuscript ref [25])
matplotlib>=3.7          # figures (confusion matrices, PR curves)
pyyaml>=6.0              # optional; JSON configs work without it
pytest>=7.0              # optional; smoke tests
datasets>=2.14           # optional; only to export CoDA from Hugging Face
# Phase 2+ only (NOT needed for Phase 1):
# torch>=2.0
# transformers>=4.35
# shap>=0.43
# lime>=0.2
# Phase 3 (explainable scoring) — CPU OK
shap>=0.44
lime>=0.2
# Phase 2 (multilingual) — GPU
transformers>=4.40
torch>=2.0
# Phase 5 (STIX/TAXII)
stix2>=3.0
taxii2-client>=2.3
tabulate>=0.9  # for build_all_tables markdown output

# ---------------------------------------------------------------------------
# Statistical validation (manuscript Section 8.13 / src/exp_stats.py)
# PINNED for reproducibility. The stats layer (McNemar, paired bootstrap,
# Wilson CI, Friedman+Nemenyi, Holm-Bonferroni) depends ONLY on the four
# packages below. These exact versions are a known-good, mutually compatible
# CPU-only stack; install them to reproduce stats_results.json bit-for-bit
# (results are deterministic given seed=42).
#
#   pip install numpy==1.26.4 scipy==1.11.4 scikit-learn==1.3.2 pandas==2.1.4
#
# Note: pin numpy<2 here because scikit-learn 1.3.x is built against the
# NumPy 1.x ABI; numpy 2.x can break it. If you already run numpy 2.x with a
# matching scikit-learn>=1.4, that combination is also fine.
# numpy==1.26.4
# scipy==1.11.4
# scikit-learn==1.3.2
# pandas==2.1.4
# ---------------------------------------------------------------------------
